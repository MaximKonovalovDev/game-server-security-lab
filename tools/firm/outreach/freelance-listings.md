# Upwork/Fiverr listing copy — paste and publish

## Profile headline (pick one)
- "MCP & AI plugin security auditor — 1st-line defense for your AI tools"
- "Game server & netcode security reviews for indie multiplayer devs"
- "Website security audit with fixes — WordPress, WooCommerce, servers"

## Profile summary (Upwork)
```
I find real vulnerabilities and fix them — evidence over vibes.

Recent work: audited a live MCP plugin endpoint and found it answered
tools/list and tools/call with NO authentication (200 + full tool list,
expected 401). Walked the fix through: listener-level token enforcement,
fail-closed, all methods/paths.

Services:
- MCP / AI plugin security audit (endpoint, dispatch, prompt-injection pass)
- Game netcode security review (protocol fuzz, cheat-class probes, fixes)
- Website/server security scan + applied fixes + re-scan verification
- Monthly re-scan retainer with CVE brief

You get: findings report (evidence, severity, fix), applied fixes where you
want them, verified re-scan. Written authorization + signed scope first —
always.
```

## Fiverr gigs (3 gigs, one per tier)

### Gig 1 — Website security scan with fixes ($60-150)
```
I will audit your website's security and fix what I find
- Security headers, TLS, ports, CMS/plugin checks, sensitive path exposure
- 2-day delivery: findings report + applied fixes + re-scan proof
- Perfect for WordPress/WooCommerce shops
```

### Gig 2 — MCP / AI plugin security audit ($250-600)
```
I will audit your MCP server for auth bypass, tool poisoning, prompt injection
- Endpoint auth, dispatch validation, injection corpus pass
- 3-day delivery: report + fix plan
- Who needs it: anyone selling/loading MCP servers
```

### Gig 3 — Game netcode security review ($500+ custom)
```
I will fuzz and probe your multiplayer game protocol
- Packet fuzzing, cheat-class probes (speed/teleport/forge), DoS checks
- 5-day delivery: findings + fixes
- For indie devs shipping multiplayer (ENet/UDP/WebSocket)
```

## Pricing notes
- Fiverr: start gigs at LOW price with no reviews; 1-2 fast $60 jobs for
  reviews, then raise. Upwork: proposals, $50-100 entry scan fixed price.
- Retainer upsell: "want monthly re-scans + CVE brief? $50-100/mo" — ask
  after every delivery.
